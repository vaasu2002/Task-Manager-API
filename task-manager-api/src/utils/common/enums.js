const FLAG_TYPES = {
    PENDING: 'PENDING',
    COMPLETED: 'COMPLETED',
}

const PRIORITY_LEVEL = {
    LOW: 'LOW',
    MEDIUM: 'MEDIUM',
    HIGH: 'HIGH',
}

module.exports = {
    FLAG_TYPES,
    PRIORITY_LEVEL
}